import { ensureSchema } from '@/lib/db'; import { clean, db, failure, failureFor, json, nextOrderNumber, noStore, orderPayload } from '@/lib/api'; import { retailerForOrder } from '@/lib/retailers'; import { kolkataDate } from '@/lib/orders';
export const runtime='nodejs'; export const dynamic='force-dynamic'; export const revalidate=0;
export async function GET(){try{await ensureSchema();return json(await db()`SELECT * FROM glass_rx_orders ORDER BY created_at DESC`,noStore)}catch(error){return failureFor(error)}}
export async function POST(request){try{await ensureSchema();const body=await request.json();if(!clean(body.lens_type)||!body.ref_id)return failure('Lens Type and Ref. Name are required.');const retailer=await retailerForOrder(body.ref_id);if(!retailer)return failure('Select a valid Ref. Name.');const eye=orderPayload(body);const orderNumber=await nextOrderNumber('glass');const rows=await db()`INSERT INTO glass_rx_orders(order_number,order_no,order_date,retailer_id,ref_name,ref_mobile,it_code,lens_type,dia,right_eye,left_eye,status) VALUES(${orderNumber},${orderNumber},${clean(body.order_date)||kolkataDate()},${retailer.id},${retailer.name},${retailer.mobile},${clean(body.it_code)},${clean(body.lens_type)},${clean(body.dia)},${eye.right},${eye.left},'Pending') RETURNING *`;return json(rows[0],{status:201})}catch(error){return failureFor(error,'Unable to create Glass RX order')}}
